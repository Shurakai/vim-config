XPTemplate priority=personal+

XPT class " Plain old PHP-Class
XSET me=fileRoot()
namespace `namespace^

/**
 * `classDescription^
 *
 * @author `$author^ < `$email^ >
 * @copyright by livando.com `strftime("%Y")
 * @package `package^
 * @since : `strftime("%d. %b %Y")^
 */
class `me^ {
    `cursor^
}
